# FT-UX-V11-VISUAL-E2E-ACCEPT-001 - FundTrader visual smoke + allocation stream acceptance

Created: 2026-06-17T01:59:00+08:00
PM: Codex
Executor: Claude Code via cc-switch

## Goal

Run an end-to-end visual and runtime acceptance of the existing FundTrader UX scope with no additional UI refactors.
Confirm that Home, Fund detail, allocation wizard/overview screens are readable with populated data and that the allocation stream flow surfaces progress, timeout, and error states without regressions.

## Context

- Current repo: FundTrader
- Current task owner: Codex PM
- Coding agent role: implementer only
- Important background:
  - Keep unrelated dirty worktree changes untouched.
  - If repo state has drifted from this handoff, stop and report before editing.

## Approved Scope

Files or areas the coding agent may edit:

- README/docs/pm runtime artifacts generated by acceptance run only:
  - `docs/pm/reports/FT-UX-V11-VISUAL-E2E-ACCEPT-001.md`
- `docs/pm/reviews` and `docs/pm/logs` entries for this task if needed by PM tooling.

No source/business files may be modified.

Files or areas the coding agent must not edit:

- Unrelated generated assets
- Deployment output unless explicitly requested
- Git history, branches, tags, or remotes
- Anything outside this handoff without PM approval

## Allowed Files

- `docs/pm/reports/FT-UX-V11-VISUAL-E2E-ACCEPT-001.md`
- `docs/pm/reviews/FT-UX-V11-VISUAL-E2E-ACCEPT-001.review.md`
- `docs/pm/reviews/FT-UX-V11-VISUAL-E2E-ACCEPT-001.review.json`
- `docs/pm/logs/FT-UX-V11-VISUAL-E2E-ACCEPT-001.jsonl`
- `docs/pm/reports/FT-UX-V11-VISUAL-E2E-ACCEPT-001.md` (overwrite permitted)

## Required Repo Check Before Editing

Run and summarize:

```powershell
git log --oneline -5
git rev-parse --short HEAD
git status --short --untracked-files=all
```

If the status contains unrelated changes, preserve them and continue only inside
the approved scope.

## Implementation Tasks

1. Run required repo check command block and summarize status drift.
2. Execute lightweight runtime smoke checks with existing local services:
   - Home page desktop and mobile smoke.
   - Fund detail page for code `512100` (desktop + mobile), then one additional id if available.
   - Allocation overview and allocation wizard entry interactions.
   - Allocation stream endpoint `/allocation/generate/stream` progress frame behavior for one complete flow (if backend available).
3. Produce an acceptance report with pass/fail per item and blockers (including data availability blockers).

## Contracts And Design Decisions

List stable interfaces, API contracts, UX rules, data-shape rules, or module
boundaries that the coding agent must preserve.

- Keep current feature behavior unchanged; do not add or remove UI blocks.
- Accept only data-rendering/visibility verification; any functional regression must be logged and returned as `decision_needed`.
- Preserve existing queue and no-deploy constraints: no API contract changes.
- If backend API is unavailable, mark item as blocked and capture exact error.

## Validation

Commands or checks the coding agent must run:

```powershell
git log --oneline -5
git rev-parse --short HEAD
git status --short --untracked-files=all

cd frontend
npm.cmd run check
npm.cmd run build
cd ..

cd backend
python -m py_compile app/api/allocation.py
python -m pytest tests/test_fund_research_report.py -q

# Visual smoke commands should be executed in the same environment currently used by previous acceptance tasks.
```

Expected result:

- All above checks pass.
- Home and Fund detail render without layout overflow errors.
- Allocation wizard/overview and stream smoke is either pass or explicitly blocked by environment.

## Stop Conditions

Stop and write a report instead of guessing when:

- The implementation requires changing files outside the approved scope.
- A product, architecture, data-contract, or deployment decision is missing.
- Validation fails for reasons unrelated to this task.
- The repo state is too different from the assumptions in this handoff.

## Final Report Required

Write `docs/pm/reports/FT-UX-V11-VISUAL-E2E-ACCEPT-001.md` with:

Start the report with this short machine-readable digest:

```markdown
## PM Digest

Status: complete | needs_fix | blocked | decision_needed
Changed: file1, file2
Validation: passed | failed | skipped - command names only
Risk: none | brief risk
Decision: none | exact PM/user question
Next: accept | create_hotfix | run_followup | ask_user
```

Then include:

1. Status
2. Summary
3. Files changed
4. Validation commands and results
5. Scope / safety
6. Open risks or PM decisions needed
7. Recommended next action

Do not include hidden chain-of-thought or `<think>` blocks.
Keep successful command output summarized. Include full output only for failures.
